#pragma once

namespace combat {

namespace reduce_sum {

template<class T>
T reduce_sum_host(T const *data, std::size_t length) {
    /*
        提示: 使用多核并行
    */
}

} // namespace reduce_sum

} // namespace combat